# 基于遗忘曲线的智能英语单词学习系统课程设计方案

## 一、项目名称

推荐项目名称：

**基于遗忘曲线的智能英语单词学习系统**

也可以写成更工程化的名称：

**基于 Vue3 + Spring Boot + Python 的智能背单词与复习推荐系统**

---

## 二、项目背景

传统背单词软件往往只提供单词展示、收藏和简单测试功能，学习过程比较固定，不能很好地根据用户的掌握情况动态安排复习。

本项目面向英语单词学习场景，设计并实现一个支持自定义词库导入、单词发音、学习计划、测试练习、错词本、遗忘曲线复习推荐和学习数据可视化的智能背单词系统。

系统的核心思想是：

```text
学习新词 → 测试掌握情况 → 生成复习计划 → 到期提醒复习 → 统计学习数据 → 调整推荐
```

通过这个闭环，系统不仅能帮助用户背单词，还能根据用户的学习记录动态推荐复习内容。

---

## 三、项目总体目标

本系统主要实现以下目标：

1. 支持用户注册、登录和个人学习数据管理。
2. 支持系统词库和用户自定义词库。
3. 支持标准 Excel / CSV 词库导入。
4. 提供 AI 辅助格式转换提示词，帮助用户整理非标准单词资料。
5. 支持单词发音，优先播放真实音频，失败时使用浏览器语音朗读兜底。
6. 支持单词信息自动补全，包括中文释义、英文释义、音标、例句、发音和辅助记忆图片。
7. 支持例句单词点击查询，用户点击例句中不会的单词后，可通过小浮窗查看简略释义并播放读音。
8. 支持学习计划、每日学习任务和复习任务。
9. 支持测试模式、听写模式和错词本。
10. 基于遗忘曲线或简化间隔重复算法生成复习推荐。
11. 支持学习量、复习量、正确率、掌握状态、打卡热力图等可视化图表。
12. 支持管理员或用户管理词库、单词和学习记录。

---

## 四、推荐技术栈

### 1. 前端

```text
Vue3
Vite
JavaScript / TypeScript
Element Plus
ECharts
Axios
Vue Router
Pinia
```

前端主要负责：

- 页面展示
- 用户交互
- 单词学习页面
- 发音播放
- 文件上传
- 导入预览和编辑
- 学习数据图表展示

---

### 2. 后端

```text
Java
Spring Boot
Spring MVC
MyBatis / MyBatis-Plus
Spring Security / JWT
MySQL
Redis，可选
Swagger / Knife4j，可选
```

Java 后端主要负责：

- 用户登录注册
- 权限控制
- 单词库管理
- 单词导入
- 学习记录管理
- 测试记录管理
- 错词本管理
- 调用 Python 智能服务

---

### 3. Python 服务

```text
Python
FastAPI / Flask
Pandas
NumPy
```

Python 模块主要负责：

- 遗忘曲线计算
- 今日复习推荐
- 掌握度评分计算
- 易混词分析，可选
- 学习数据统计分析

---

### 4. 外部免费资源与 API

```text
Free Dictionary API：英文释义、音标、真实发音、部分例句
浏览器 Web Speech API：发音兜底
ECDICT：本地英汉词典，主要用于中文释义
Pexels API：辅助记忆图片，可选
Tatoeba：补充例句，可选
```

说明：

- 中文释义优先使用本地词库和 ECDICT，不建议完全依赖在线 API。
- 单词真实音频优先使用 Free Dictionary API。
- 如果真实音频缺失或播放失败，前端使用浏览器 Web Speech API 自动朗读。
- Pexels API 适合用来给具体名词展示辅助记忆图片，例如 apple、cat、mountain。
- 抽象词不一定适合图片辅助记忆，例如 responsibility、development、important。

---

### 5. 数据库

```text
MySQL
```

主要存储：

- 用户信息
- 词库信息
- 单词信息
- 用户学习记录
- 复习计划
- 测试记录
- 错词记录
- 发音音频地址
- 学习统计数据

---

## 五、系统整体架构

推荐采用前后端分离架构：

```text
Vue3 前端
   ↓
Spring Boot 后端
   ↓
MySQL 数据库

Spring Boot 后端
   ↓ HTTP 调用
Python FastAPI 智能推荐服务
```

示例流程：

```text
用户进入今日学习页面
↓
Vue3 请求 Java 后端
↓
Java 后端读取用户学习记录
↓
Java 调用 Python 服务计算今日复习单词
↓
Python 返回推荐结果
↓
Java 返回给前端
↓
前端展示今日学习和复习任务
```

---

## 六、系统核心功能模块

## 1. 用户模块

### 功能

- 用户注册
- 用户登录
- 退出登录
- 修改个人信息
- 查看学习数据
- 管理个人词库

### 说明

用户登录后，可以拥有自己的学习记录、词库、错词本和复习计划。

---

## 2. 词库管理模块

### 功能

- 查看系统内置词库
- 创建自定义词库
- 查看某个词库下的单词
- 修改词库名称和描述
- 删除自定义词库
- 查询词库学习进度

### 示例词库

```text
四级核心词汇
六级高频词汇
考研英语词汇
雅思词汇
托福词汇
我的自定义词库
老师课堂词汇
```

---

## 3. 单词学习模块

### 功能

- 单词卡片展示
- 英文单词
- 中文释义
- 音标
- 词性
- 例句
- 发音按钮
- 标记熟悉 / 模糊 / 不认识
- 加入错词本
- 加入收藏
- 进入下一个单词

### 学习状态

```text
未学习
学习中
待复习
已掌握
易错词
```

---

## 4. 单词发音模块

### 设计方案

采用：

```text
真实音频优先 + 浏览器 TTS 兜底
```

具体流程：

```text
用户点击发音按钮
↓
先检查数据库是否有 audio_us 或 audio_uk
↓
如果有音频地址，直接播放
↓
如果播放失败，调用浏览器 Web Speech API 朗读
↓
如果数据库没有音频地址，请求免费词典 API
↓
如果词典 API 返回音频，播放音频并保存音频地址
↓
如果词典 API 没有音频，使用浏览器 TTS 兜底
```

### 推荐功能

- 支持美音按钮
- 支持英音按钮
- 支持音频播放失败自动降级
- 支持数据库缓存音频地址

### 前端发音兜底示例

```javascript
function speakWord(word) {
  const utterance = new SpeechSynthesisUtterance(word)
  utterance.lang = 'en-US'
  utterance.rate = 0.8
  window.speechSynthesis.speak(utterance)
}
```


### 答辩表述

```text
系统支持单词真人发音播放。当词典 API 返回音频资源时，系统优先播放在线音频；
当音频资源缺失或播放失败时，系统自动调用浏览器 SpeechSynthesis 接口进行英文朗读，
保证每个单词都可以正常发音。
```

---

## 5. 单词信息自动补全与辅助记忆模块

这个模块是在发音功能基础上的进一步扩展，目标是让用户在学习单词时不仅能看到释义和音标，还能看到例句、图片，并能在例句中点击不会的单词进行快速查询。

### 5.1 设计目标

```text
输入或导入单词
↓
优先查询本地词库和 ECDICT 获取中文释义
↓
调用 Free Dictionary API 补全英文释义、音标、真实发音和部分例句
↓
如有需要，调用 Pexels API 获取辅助记忆图片
↓
将补全结果缓存到数据库
↓
前端展示为单词学习卡片
```

这个模块可以包装成：

```text
单词信息自动补全模块
```

而不是简单写成“调用 API”。这样在报告和答辩里会更专业。

---

### 5.2 中文释义获取方案

中文释义建议采用：

```text
用户导入词库中的 meaning 字段
+
本地 ECDICT 英汉词典
```

优先级建议：

```text
1. 当前用户词库中的 meaning
2. 系统内置词库中的中文释义
3. 本地 ECDICT 查询结果
4. 如果都没有，则显示“暂无中文释义”
```

说明：

```text
ECDICT 适合作为本地英汉词典数据源，可以导入 MySQL。
正式项目中不建议让前端直接保存完整 ECDICT 数据，
而应该由 Spring Boot 后端提供查询接口。
```

后端查询流程示例：

```text
前端点击单词
↓
请求 GET /api/dict/simple?word=example
↓
Spring Boot 查询本地 ECDICT 表
↓
返回中文释义、音标、词性等简略信息
↓
前端用小浮窗展示
```

---

### 5.3 英文释义、音标、真实发音和例句

推荐继续使用 Free Dictionary API。

可以获取：

```text
word：单词
phonetics：音标和发音
audio：真实发音音频
meanings：词性和英文释义
definitions：定义
example：部分单词会返回例句
synonyms：同义词，可选
antonyms：反义词，可选
```

使用策略：

```text
1. 数据库已有音标、英文释义、音频、例句时，优先使用数据库缓存
2. 数据库缺少信息时，再调用 Free Dictionary API
3. API 返回结果后，保存到数据库
4. API 没有例句时，可以使用人工例句、用户导入例句或后期接入 Tatoeba
```

---

### 5.4 例句单词点击小浮窗

这是一个很适合展示的交互功能。

用户在学习单词时，系统显示例句：

```text
Education is important for personal development.
```

如果用户不会其中的 `development`，可以点击这个单词，旁边弹出一个小浮窗：

```text
development
/dɪˈveləpmənt/
发展；开发
[播放读音]
```

### 小浮窗内容

```text
单词
音标
简略中文释义
播放读音按钮
可选：加入生词本按钮
```

### 查询优先级

```text
1. 先查本地 ECDICT
2. 如果 ECDICT 没有，再查系统已有 word 表
3. 如果仍然没有，可以调用 Free Dictionary API 获取英文释义
4. 发音优先使用真实音频，没有则浏览器朗读
```

### 前端实现思路

```text
1. 前端把例句按英文单词切分
2. 每个英文单词渲染成可点击 span
3. 用户点击单词
4. 前端请求后端简略词典接口
5. 返回释义和音标
6. 在点击位置附近显示 popover 小浮窗
```

### Vue 页面中的交互效果

```text
例句文字中，每个单词都可以点击；
点击后不跳转页面，而是在旁边弹出简略词典卡片；
用户可以直接听读音、看释义，继续学习当前单词。
```

### 答辩表述

```text
系统在例句学习场景中加入了快速查词功能。
当用户阅读英文例句时，可以点击不认识的单词，系统会优先查询本地 ECDICT 词典，
并通过小浮窗展示该词的中文释义、音标和发音按钮。
该设计降低了用户阅读例句的理解成本，提高了学习连续性。
```

---

### 5.5 Pexels 辅助记忆图片

Pexels API 可以作为可选增强功能，用于给具体名词展示图片，帮助用户建立图像记忆。

适合图片辅助记忆的词：

```text
apple
cat
dog
book
mountain
river
flower
computer
```

不太适合图片辅助记忆的词：

```text
responsibility
important
development
accurate
education
```

### 图片获取流程

正式项目中建议这样做：

```text
Vue3 前端
↓
请求 Spring Boot 后端
↓
Spring Boot 后端携带 Pexels API Key 请求 Pexels
↓
Pexels 返回图片 URL、摄影师、图片链接
↓
后端保存或缓存 image_url、image_source、photographer 等信息
↓
前端展示图片
```

不要把 Pexels API Key 写死在前端代码里。原因是：

```text
前端代码会暴露给用户
API Key 容易被复制和滥用
正式项目中应由后端统一保存和调用
```

### 图片展示要求

Pexels 图片一般需要展示来源信息，可以在图片下方显示：

```text
Photo by 摄影师 on Pexels
```

数据库中建议保存：

```text
image_url
image_source
image_photographer
image_page_url
```

---

### 5.6 单词信息自动补全的完整流程

```text
用户导入或手动添加单词
↓
系统检查 word、meaning 等基础字段
↓
如果中文释义缺失，查询本地 ECDICT
↓
如果音标、英文释义、发音缺失，调用 Free Dictionary API
↓
如果例句缺失，优先使用 Free Dictionary API 返回的 example
↓
如果启用图片辅助记忆，后端调用 Pexels API 获取图片
↓
把补全后的信息保存到 word 表
↓
用户学习时直接从数据库读取，减少重复请求
```

### 推荐缓存策略

```text
1. 第一次查询外部 API 后保存结果
2. 下次学习同一个单词时直接读取数据库
3. 音频地址、图片地址、例句都可以缓存
4. 对外部 API 请求失败的单词，可以记录失败时间，避免短时间内重复请求
```

---

### 5.7 这个模块的课设难度控制

基础版：

```text
Free Dictionary API 获取音标、英文释义、真实发音
浏览器 TTS 兜底
例句展示
例句单词点击小浮窗，使用后端本地 ECDICT 查询
```

进阶版：

```text
Pexels API 获取辅助记忆图片
图片缓存
加入生词本
Tatoeba 补充例句
```

建议你们课设优先实现：

```text
1. 发音
2. 例句展示
3. 例句单词点击小浮窗
4. ECDICT 本地释义查询
5. Pexels 图片作为加分功能
```

---

## 6. 词库导入模块

本项目的词库导入模块最终设计为：

```text
真正的数据导入口只有一个：标准 Excel / CSV 导入
AI 提示词转换导入不是直接导入，而是辅助用户把非标准资料转换成标准 Excel / CSV
```

### 6.1 标准 Excel / CSV 导入

这是系统真正的导入口，属于必做功能。

### 标准字段

| 字段名 | 含义 | 是否必填 | 说明 |
|---|---|---|---|
| word | 英文单词或短语 | 必填 | 例如 apple |
| meaning | 中文释义 | 必填 | 例如 苹果 |
| phonetic | 音标 | 可选 | 例如 /ˈæpəl/ |
| partOfSpeech | 词性 | 可选 | 例如 n.、v.、adj. |
| example | 英文例句 | 可选 | 例如 I eat an apple. |
| chapter | 章节 | 可选 | 例如 Unit 1 |
| difficulty | 难度 | 可选 | 1 到 5，默认 3 |

### CSV 示例

```csv
word,meaning,phonetic,partOfSpeech,example,chapter,difficulty
apple,苹果,/ˈæpəl/,n.,I eat an apple.,Unit 1,1
abandon,放弃；抛弃,/əˈbændən/,v.,He abandoned the plan.,Unit 1,3
```

---

### 6.2 AI 辅助格式转换

AI 辅助格式转换不是系统直接调用 AI，也不是第二个真正的导入口。

它的作用是：

```text
帮助用户把图片、PDF、截图、文本等非标准单词资料，
借助外部 AI 整理成系统可以识别的标准 Excel / CSV 格式。
```

### 用户流程

```text
用户有一份单词资料，可能是图片 / PDF / 文本 / 截图
↓
用户打开系统的 AI 辅助格式转换页面
↓
复制系统提供的标准提示词
↓
把资料和提示词发给 ChatGPT / Kimi / 通义 / 文心等 AI
↓
AI 输出标准 CSV 表格内容
↓
用户复制到 Excel 或保存为 CSV 文件
↓
回到系统上传 Excel / CSV
↓
系统解析、预览、校验
↓
用户修改错误数据
↓
确认导入
```

### AI 提示词示例

```text
你是一个英语单词书整理助手。请从我提供的单词资料中提取单词信息，并整理成 CSV 表格格式。

要求：
1. 只提取英语单词或短语。
2. CSV 表头必须严格使用：
word,meaning,phonetic,partOfSpeech,example,chapter,difficulty

3. 字段说明：
word：英文单词或短语，必填
meaning：中文释义，必填
phonetic：音标，如果没有请留空
partOfSpeech：词性，例如 n.、v.、adj.、adv.，如果没有请留空
example：英文例句，如果没有请留空
chapter：所属章节或单元，如果无法判断，请填“默认词库”
difficulty：难度，1 到 5 的整数。如果无法判断，请填 3

4. 不要输出解释。
5. 不要输出 Markdown。
6. 不要使用代码块。
7. 不要添加资料中不存在的单词。
8. 如果释义或例句中有英文逗号，请用英文双引号包裹该字段，保证 CSV 格式合法。

输出示例：
word,meaning,phonetic,partOfSpeech,example,chapter,difficulty
apple,苹果,/ˈæpəl/,n.,I eat an apple.,Unit 1,1
abandon,放弃；抛弃,/əˈbændən/,v.,He abandoned the plan.,Unit 1,3

下面是我的单词资料：
```

---

## 7. 导入预览和校验模块

这是词库导入功能中非常重要的部分。

### 为什么需要预览和校验

因为用户上传的 Excel / CSV 或 AI 生成的数据可能存在错误，例如：

```text
word 为空
meaning 为空
difficulty 不是 1 到 5
文件内部有重复单词
当前词库中已经存在该单词
word 字段混入中文
CSV 格式错误
```

因此，上传文件后不能直接入库，应该先进入预览和校验页面。

### 推荐流程

```text
用户上传 Excel / CSV
↓
后端解析文件
↓
生成预览数据
↓
前端展示可编辑表格
↓
系统标记错误数据和重复数据
↓
用户手动修改
↓
点击确认导入
↓
后端正式保存到数据库
```

### 预览页面显示内容

```text
共解析 120 个单词
可导入 112 个
重复单词 5 个
格式错误 3 个
```

### 用户可执行操作

```text
修改错误数据
删除某行数据
跳过重复单词
覆盖已有单词
合并释义
取消导入
确认导入
```

### 答辩表述

```text
本系统采用统一的标准词库导入方案。用户可以通过 Excel 或 CSV 文件导入自定义单词书。
对于图片、PDF、截图或非标准文本等资料，系统提供 AI 格式转换提示词，
用户可借助外部 AI 将资料整理为系统标准表格格式，再通过统一导入口上传。
系统在入库前会进行预览、校验、去重和用户手动修正，从而提高导入数据的准确性和可控性。
```

---

## 8. 学习计划模块

### 功能

- 用户选择词库
- 设置学习目标
- 设置每天新学单词数量
- 设置计划完成时间
- 系统生成每日学习任务
- 支持动态调整计划

### 示例

```text
用户选择：考研英语 Unit 1
词库总量：300 个单词
目标：10 天学完
系统建议：每天新学 30 个单词
```

如果用户某天没有完成，系统可以重新计算：

```text
你今天建议新学 38 个单词，复习 65 个单词
```

---

## 9. 复习推荐模块

### 核心思想

系统根据用户的答题结果、上次复习时间、错误次数和记忆强度，计算哪些单词今天应该复习。

---

### 简化遗忘曲线公式

课程设计中可以使用一个简单好讲的公式：

```text
R = e^(-t / S)
```

其中：

```text
R：记忆保持率
t：距离上次复习过去的天数
S：记忆强度
```

规则示例：

```text
用户答对：S = S + 1
用户答错：S = max(1, S - 1)
```

当：

```text
R < 0.6
```

说明该单词应该进入复习列表。

---

### 简化间隔重复规则

也可以采用更容易实现的规则：

```text
第一次答对：1 天后复习
第二次答对：3 天后复习
第三次答对：7 天后复习
第四次答对：15 天后复习
第五次答对：30 天后复习
答错：明天重新复习
```

### 用户反馈等级

用户学习或测试后可以选择：

```text
完全不会 → 明天复习
模糊记得 → 2 天后复习
基本记得 → 5 天后复习
非常熟练 → 10 天后复习
```

---

## 10. 测试模式模块

### 测试题型

```text
英译中
中译英
选择题
拼写填空
例句填空
听音选词
听写模式
```

### 示例题型一：英译中

```text
apple 的中文意思是？
A. 苹果
B. 香蕉
C. 橙子
D. 梨
```

### 示例题型二：听音选词

```text
播放发音，请选择你听到的单词：
A. apple
B. apply
C. ample
D. appeal
```

### 示例题型三：听写模式

```text
系统播放单词发音
用户输入听到的单词
系统判断拼写是否正确
答错则加入错词本
```

例如：

```text
播放：beautiful
用户输入：beutiful
系统提示：拼写错误，正确答案 beautiful
```

---

## 11. 错词本模块

### 功能

- 自动记录测试错误单词
- 手动加入错词本
- 查看错误次数
- 查看最近错误时间
- 按错误原因分类
- 从错词本中移除
- 针对错词生成专项测试

### 错误原因分类

```text
拼写错误
意思混淆
发音不熟
长时间未复习
测试多次错误
```

### 可视化

可以做错词原因饼图：

```text
拼写错误：40%
意思混淆：35%
发音不熟：15%
其他：10%
```

---

## 12. 掌握度评分模块

每个用户对每个单词都有一个掌握度分数：

```text
0 - 100 分
```

### 评分规则示例

```text
答对一次：+10
答错一次：-15
连续答对：额外 +5
长时间未复习：每天 -2
听写正确：+15
```

### 示例

```text
apple：掌握度 90
abandon：掌握度 45
accurate：掌握度 70
```

掌握度较低的单词会优先进入复习推荐。

---

## 13. 数据可视化模块

前端推荐使用 ECharts。

### 12.1 每日学习量柱状图

展示每天新学了多少个单词。

```text
6 月 1 日：20 个
6 月 2 日：30 个
6 月 3 日：15 个
6 月 4 日：25 个
```

---

### 12.2 每日复习量折线图

展示每天复习了多少个单词。

可以与每日学习量放在同一个仪表盘中。

---

### 12.3 正确率变化折线图

记录每次测试的正确率变化。

```text
第 1 次测试：60%
第 2 次测试：72%
第 3 次测试：81%
第 4 次测试：85%
```

---

### 12.4 单词掌握状态饼图

展示用户当前单词掌握状态：

```text
未学习
学习中
待复习
已掌握
易错词
```

---

### 12.5 学习打卡热力图

类似 GitHub 贡献图。

```text
颜色越深，表示当天学习越多
颜色越浅，表示当天学习越少
没有颜色，表示当天没学习
```

---

### 12.6 遗忘曲线图

展示某个单词的记忆保持率变化。

```text
刚学完：100%
1 天后：75%
3 天后：50%
7 天后：30%
复习后：重新升高到 100%
```

---

## 七、系统首页设计

首页建议做成学习仪表盘。

### 首页卡片

```text
今日新学目标：20 个
今日应复习：35 个
今日已完成：18 / 55
当前连续打卡：6 天
总掌握单词：326 个
高风险遗忘单词：8 个
```

### 首页图表

```text
最近 7 天学习量柱状图
最近 7 天复习量折线图
正确率变化折线图
单词掌握状态饼图
学习打卡热力图
```

---

## 八、数据库设计初稿

## 1. 用户表 user

```sql
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  nickname VARCHAR(50),
  email VARCHAR(100),
  avatar VARCHAR(255),
  create_time DATETIME,
  update_time DATETIME
);
```

---

## 2. 词库表 word_book

```sql
CREATE TABLE word_book (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT,
  name VARCHAR(100) NOT NULL,
  description VARCHAR(255),
  source_type VARCHAR(30),
  word_count INT DEFAULT 0,
  create_time DATETIME,
  update_time DATETIME
);
```

source_type 可选值：

```text
system：系统内置
excel：Excel 导入
csv：CSV 导入
ai_csv：AI 辅助转换后导入
manual：手动创建
```

---

## 3. 单词表 word

```sql
CREATE TABLE word (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  book_id BIGINT NOT NULL,
  word VARCHAR(100) NOT NULL,
  meaning TEXT NOT NULL,
  english_definition TEXT,
  phonetic VARCHAR(100),
  part_of_speech VARCHAR(50),
  example TEXT,
  example_translation TEXT,
  chapter VARCHAR(100),
  difficulty INT DEFAULT 3,
  audio_us VARCHAR(500),
  audio_uk VARCHAR(500),
  image_url VARCHAR(500),
  image_source VARCHAR(100),
  image_photographer VARCHAR(100),
  image_page_url VARCHAR(500),
  create_time DATETIME,
  update_time DATETIME
);
```

---

## 4. 本地英汉词典表 ecdict_word

用于保存 ECDICT 或其他本地英汉词典数据，主要服务于中文释义查询和例句单词点击小浮窗。

```sql
CREATE TABLE ecdict_word (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  word VARCHAR(100) NOT NULL UNIQUE,
  phonetic VARCHAR(100),
  translation TEXT,
  definition TEXT,
  pos VARCHAR(50),
  collins INT,
  oxford INT,
  tag VARCHAR(255),
  exchange TEXT,
  create_time DATETIME
);
```

说明：

```text
word：英文单词
phonetic：音标
translation：中文释义
definition：英文释义
pos：词性
exchange：单词变形，例如过去式、复数等
```

---

## 5. 用户单词学习记录表 user_word

```sql
CREATE TABLE user_word (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  word_id BIGINT NOT NULL,
  status VARCHAR(30),
  mastery_score INT DEFAULT 0,
  memory_strength DOUBLE DEFAULT 1,
  review_count INT DEFAULT 0,
  correct_count INT DEFAULT 0,
  wrong_count INT DEFAULT 0,
  last_review_time DATETIME,
  next_review_time DATETIME,
  create_time DATETIME,
  update_time DATETIME
);
```

---

## 6. 学习记录表 study_record

```sql
CREATE TABLE study_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  word_id BIGINT NOT NULL,
  action_type VARCHAR(30),
  result VARCHAR(30),
  study_time DATETIME
);
```

action_type 示例：

```text
learn
review
test
dictation
```

result 示例：

```text
correct
wrong
known
unknown
fuzzy
```

---

## 7. 测试记录表 test_record

```sql
CREATE TABLE test_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  book_id BIGINT,
  test_type VARCHAR(30),
  total_count INT,
  correct_count INT,
  wrong_count INT,
  accuracy DOUBLE,
  create_time DATETIME
);
```

---

## 8. 错词表 wrong_word

```sql
CREATE TABLE wrong_word (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  word_id BIGINT NOT NULL,
  reason VARCHAR(50),
  wrong_count INT DEFAULT 1,
  last_wrong_time DATETIME,
  create_time DATETIME,
  update_time DATETIME
);
```

---

## 9. 学习计划表 study_plan

```sql
CREATE TABLE study_plan (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  book_id BIGINT NOT NULL,
  daily_new_count INT,
  start_date DATE,
  end_date DATE,
  status VARCHAR(30),
  create_time DATETIME,
  update_time DATETIME
);
```

---

## 九、接口设计初稿

## 1. 用户接口

```text
POST /api/user/register
POST /api/user/login
GET  /api/user/info
PUT  /api/user/info
POST /api/user/logout
```

---

## 2. 词库接口

```text
GET    /api/word-book/list
POST   /api/word-book/create
GET    /api/word-book/{id}
PUT    /api/word-book/{id}
DELETE /api/word-book/{id}
GET    /api/word-book/{id}/words
```

---

## 3. 词库导入接口

```text
GET  /api/word-book/template
POST /api/word-book/import/preview
POST /api/word-book/import/confirm
```

说明：

```text
/api/word-book/import/preview：上传 Excel / CSV，后端解析后返回预览和校验结果
/api/word-book/import/confirm：用户确认后正式导入数据库
```

---

## 4. 单词接口

```text
GET  /api/word/{id}
POST /api/word/create
PUT  /api/word/{id}
DELETE /api/word/{id}
GET  /api/word/search
```

---

## 5. 发音接口

```text
GET  /api/word/{id}/audio
POST /api/word/{id}/audio/cache
```

也可以把发音逻辑主要放在前端，后端只负责保存音频地址。

---

## 6. 单词信息补全接口

```text
POST /api/word/complete
GET  /api/word/{id}/detail
GET  /api/dict/simple?word=example
GET  /api/word/{id}/image
POST /api/word/{id}/image/cache
```

说明：

```text
/api/word/complete：对某个单词执行信息补全，包括中文释义、英文释义、音标、例句和音频
/api/dict/simple：用于例句点击小浮窗，返回简略中文释义、音标和词性
/api/word/{id}/image：获取单词辅助记忆图片
/api/word/{id}/image/cache：保存或更新图片缓存
```

---

## 7. Pexels 图片代理接口

```text
GET /api/image/pexels/search?word=apple
```

说明：

```text
正式项目中 Pexels API Key 应该保存在后端配置文件中。
前端不要直接请求 Pexels，也不要把 Key 写到 Vue 代码里。
```

---

## 8. 学习接口

```text
GET  /api/study/today
POST /api/study/learn
POST /api/study/review
GET  /api/study/progress
```

---

## 9. 测试接口

```text
POST /api/test/generate
POST /api/test/submit
GET  /api/test/history
```

---

## 10. 错词本接口

```text
GET    /api/wrong-word/list
POST   /api/wrong-word/add
DELETE /api/wrong-word/{id}
GET    /api/wrong-word/statistics
```

---

## 11. 数据统计接口

```text
GET /api/stat/overview
GET /api/stat/daily-study
GET /api/stat/daily-review
GET /api/stat/accuracy
GET /api/stat/mastery
GET /api/stat/heatmap
```

---

## 12. Python 智能服务接口

```text
POST /python/recommend/review
POST /python/memory/calculate
POST /python/mastery/calculate
POST /python/stat/analyze
```

---

## 十、Python 智能服务设计

## 1. 今日复习推荐

输入：

```json
{
  "userId": 1,
  "words": [
    {
      "wordId": 101,
      "lastReviewDays": 3,
      "memoryStrength": 2.0,
      "wrongCount": 1,
      "masteryScore": 60
    }
  ]
}
```

输出：

```json
{
  "reviewWordIds": [101, 105, 108],
  "highRiskWordIds": [101]
}
```

---

## 2. 记忆保持率计算

公式：

```text
R = e^(-t / S)
```

Python 示例：

```python
import math

def memory_rate(days, strength):
    if strength <= 0:
        strength = 1
    return math.exp(-days / strength)
```

---

## 3. 掌握度计算

示例规则：

```python
def calculate_mastery(old_score, is_correct, review_count):
    score = old_score
    if is_correct:
        score += 10
        if review_count >= 3:
            score += 5
    else:
        score -= 15
    return max(0, min(100, score))
```

---

## 十一、页面设计建议

## 1. 登录注册页面

- 登录
- 注册
- 忘记密码，可选

---

## 2. 首页仪表盘

- 今日学习任务
- 今日复习任务
- 学习进度
- 数据图表

---

## 3. 词库列表页面

- 系统词库
- 我的词库
- 新建词库
- 导入词库

---

## 4. 词库导入页面

- 下载标准模板
- 查看标准字段说明
- 复制 AI 提示词
- 上传 Excel / CSV
- 进入预览校验页

---

## 5. 导入预览页面

- 表格展示解析结果
- 错误行高亮
- 重复单词提示
- 支持编辑
- 支持删除行
- 确认导入

---

## 6. 单词学习页面

- 单词卡片
- 发音按钮
- 中文释义
- 英文释义
- 音标
- 词性
- 例句
- 例句单词点击小浮窗
- 辅助记忆图片
- 认识 / 模糊 / 不认识
- 加入错词本

---

## 7. 今日复习页面

- 今日应复习单词
- 复习进度
- 答题反馈
- 下次复习时间更新

---

## 8. 测试页面

- 选择题
- 拼写题
- 听写题
- 提交结果
- 正确率展示

---

## 9. 错词本页面

- 错词列表
- 错误原因
- 错误次数
- 专项练习

---

## 10. 数据统计页面

- 学习量图表
- 复习量图表
- 正确率图表
- 掌握状态图表
- 热力图

---

## 十二、四人小组分工建议

## 成员 A：前端负责人

负责：

```text
Vue3 项目搭建
登录注册页面
首页仪表盘
词库列表页面
单词学习页面
测试页面
ECharts 图表展示
```

---

## 成员 B：Java 后端负责人

负责：

```text
Spring Boot 项目搭建
用户模块
词库模块
单词模块
学习记录模块
测试记录模块
错词本模块
接口文档
```

---

## 成员 C：Python 智能服务负责人

负责：

```text
FastAPI / Flask 服务
遗忘曲线计算
复习推荐算法
掌握度评分
学习数据分析
ECDICT 数据整理和导入方案
和 Java 后端联调
```

---

## 成员 D：导入模块与测试部署负责人

负责：

```text
Excel / CSV 模板设计
文件上传和解析
导入预览与校验
重复单词检测
Free Dictionary API 调用测试
Pexels API 图片测试
单词信息补全测试
系统测试
数据库维护
项目部署
答辩材料整理
```

---

## 十三、开发阶段规划

## 第一阶段：需求分析与原型设计

任务：

```text
确定功能模块
画系统流程图
设计页面原型
确定数据库表
确定接口规范
```

产出：

```text
需求分析文档
数据库设计文档
接口设计文档
页面原型图
```

---

## 第二阶段：基础系统开发

任务：

```text
搭建 Vue3 前端项目
搭建 Spring Boot 后端项目
完成用户登录注册
完成词库和单词基础 CRUD
完成数据库连接
```

产出：

```text
前后端基础框架
用户模块
词库模块
单词模块
```

---

## 第三阶段：核心学习功能开发

任务：

```text
单词学习页面
发音功能
单词信息自动补全
例句展示
例句单词点击小浮窗
ECDICT 简略释义查询
学习记录
测试模式
错词本
复习计划
```

产出：

```text
可正常使用的背单词流程
可以发音、看释义、看例句、点击例句单词查简略释义
```

---

## 第四阶段：词库导入功能开发

任务：

```text
标准模板下载
Excel / CSV 上传
文件解析
预览校验
用户编辑
确认导入
AI 提示词页面
```

产出：

```text
完整的自定义词库导入模块
```

---

## 第五阶段：Python 智能推荐与数据可视化

任务：

```text
遗忘曲线计算
今日复习推荐
掌握度评分
学习统计接口
ECharts 图表展示
学习热力图
Pexels 辅助记忆图片，可选
```

产出：

```text
智能复习推荐功能
学习数据仪表盘
```

---

## 第六阶段：测试、优化与答辩准备

任务：

```text
系统测试
修复 Bug
完善页面
准备演示数据
编写课程设计报告
制作答辩 PPT
准备答辩问题
```

产出：

```text
完整可演示系统
课程设计报告
答辩 PPT
项目演示脚本
```

---

## 十四、项目创新点总结

可以写进报告和 PPT 的创新点：

```text
1. 真实音频优先 + 浏览器 TTS 兜底的单词发音方案
2. 单词信息自动补全，支持中文释义、英文释义、音标、例句、音频和图片扩展
3. 中文释义优先使用本地词库和 ECDICT，减少对在线 API 的依赖
4. 例句单词点击小浮窗，用户可以快速查看生词释义并播放读音
5. Pexels 辅助记忆图片，为具体名词提供图像记忆支持
6. AI 辅助词库格式转换，支持用户快速整理非标准单词资料
7. 统一 Excel / CSV 导入入口，配合预览、校验、去重和用户手动修正
8. 基于遗忘曲线的个性化复习推荐
9. 动态掌握度评分，根据答题结果和复习情况调整单词状态
10. 听写模式，将发音功能和测试功能结合
11. 错词原因分类与专项练习
12. 学习数据可视化，包括学习量、复习量、正确率、掌握状态和打卡热力图
```

---

## 十五、项目难度控制建议

为了保证课设能够完成，建议分成“基础版”和“进阶版”。

## 基础版，必须完成

```text
用户登录注册
词库管理
单词管理
标准 Excel / CSV 导入
导入预览和校验
单词学习
单词发音
单词信息自动补全
例句点击小浮窗
测试模式
错词本
基础复习计划
基础学习统计图表
```

---

## 进阶版，有时间再做

```text
遗忘曲线精细化计算
听写模式
学习热力图
易混词推荐
Pexels 辅助记忆图片
自动补全音标和发音
成就系统
排行榜
PDF / 图片 OCR 自动识别
```

注意：

```text
图片 OCR 自动识别不建议作为必做功能。
它可以写成拓展功能，因为图片清晰度、排版和识别准确率都会影响系统稳定性。
```

---

## 十六、推荐最终答辩表述

可以这样介绍项目：

```text
本项目设计并实现了一个基于遗忘曲线的智能英语单词学习系统。
系统采用 Vue3 + Spring Boot + MySQL + Python 的前后端分离架构，
实现了用户管理、词库管理、单词学习、发音播放、单词信息自动补全、例句快速查词、测试练习、错词本、复习推荐和学习数据可视化等功能。

在词库导入方面，系统采用统一的 Excel / CSV 标准导入方案，
并提供 AI 辅助格式转换提示词，帮助用户将图片、PDF、截图或普通文本中的单词资料整理为标准表格格式。
系统在正式导入前会进行预览、校验、去重和用户手动修正，提高了导入数据的准确性和可控性。

在单词学习方面，系统优先使用本地词库和 ECDICT 提供中文释义，并通过 Free Dictionary API 补全音标、英文释义、例句和真实发音。
用户阅读例句时，可以点击不认识的单词，系统通过小浮窗展示简略释义和读音。
同时，系统可扩展接入 Pexels API，为具体名词提供辅助记忆图片。

在智能复习方面，系统基于用户的答题结果、复习次数、错误次数和距离上次复习的时间，
通过遗忘曲线模型计算单词记忆保持率，并生成个性化复习推荐。
同时，系统通过学习量、复习量、正确率、掌握状态和打卡热力图等可视化图表，
帮助用户直观了解自己的学习情况。
```

---

## 十七、后续可以继续完善的内容

等选题正式确定后，可以继续细化：

```text
1. 详细需求分析文档
2. 系统用例图
3. 数据库 E-R 图
4. 接口文档
5. 前端页面原型
6. 后端项目目录结构
7. Vue3 前端项目目录结构
8. Python 服务代码
9. Excel / CSV 导入代码
10. 答辩 PPT 大纲
11. 课程设计报告
12. 项目演示脚本
```


---

## 十八、Pexels API Key 获取与使用说明

### 1. 获取方式

用户或开发者可以进入 Pexels API 官方页面注册账号并申请 API Key。

课程设计演示时可以临时把 Key 输入到测试 Demo 页面中，但正式项目不建议这样做。

### 2. 正式项目使用方式

推荐流程：

```text
Vue3 前端请求后端接口
↓
Spring Boot 后端读取配置文件中的 Pexels API Key
↓
Spring Boot 请求 Pexels API
↓
Pexels 返回图片数据
↓
Spring Boot 返回必要的图片 URL 和来源信息给前端
```

### 3. 不建议的做法

```text
不建议把 Pexels API Key 写死在 Vue 前端代码里
不建议把 Key 提交到 GitHub / Gitee
不建议让用户直接看到 Key
```

原因：

```text
前端代码会暴露给用户
Key 可能被复制和滥用
容易造成额度被刷完
```

### 4. 建议保存的图片信息

```text
image_url：图片展示地址
image_source：图片来源，例如 Pexels
image_photographer：摄影师名称
image_page_url：图片页面链接
```

前端展示时可以在图片下方显示：

```text
Photo by 摄影师 on Pexels
```

---

## 十九、更新后的核心功能闭环

更新后，本项目的完整学习闭环可以总结为：

```text
导入词库
↓
单词信息自动补全
↓
单词学习卡片展示
↓
真实发音 / 浏览器朗读
↓
例句阅读与点击查词
↓
图片辅助记忆
↓
测试与听写
↓
错词本记录
↓
遗忘曲线复习推荐
↓
学习数据可视化
```

这个闭环比普通背单词系统更完整，既有前端交互，也有后端数据处理，还有 Python 智能推荐和外部开放 API 扩展。
